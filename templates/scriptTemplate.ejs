import MonoBehaviour from __dirname + '/engineModules/monobehaviour.js';

class <%= className %> extends MonoBehaviour {
    constructor(engineAPI) {
        super(engineAPI);
    }

    Start() {
        
    }

    Update() {
        
    }
}